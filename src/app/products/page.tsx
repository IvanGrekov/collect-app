import Head from 'next/head';

export default function Products(): JSX.Element {
    return (
        <main>
            <Head>
                <title>Collect - Products</title>
            </Head>

            <h1>Products</h1>
        </main>
    );
}
